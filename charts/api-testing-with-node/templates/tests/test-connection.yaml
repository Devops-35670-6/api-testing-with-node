apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-testing-with-node.fullname" . }}-test-connection"
  labels:
    {{- include "api-testing-with-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-testing-with-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
