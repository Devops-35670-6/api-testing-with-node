apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-testing-with-node.fullname" . }}
  labels:
    {{- include "api-testing-with-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-testing-with-node.selectorLabels" . | nindent 4 }}
