podTemplate(cloud: 'kubernetes',
        containers: [
                containerTemplate(
                        name:   'node14',
                        image:  'node:14.15.4',
                        command: 'cat',
                        ttyEnabled: true,
                        workingDir: env.WORKSPACE,
                )
        ]) {
    node(POD_LABEL){
        environment {

            CI = 'true'
        }

        stage('Checkout scm') {
            echo "Checking out scm..."
            checkout(scm)
            echo "Current dir: ${PWD}"
            echo "Workspace: " + env.WORKSPACE
        }

        stage('Build'){
            container('node14') {
                sh 'npm ci'
            }
        }

        stage('Test') {
            container('node14') {
                try {
                    sh "${env.WORKSPAC}/jenkins/scripts/start.sh"
                    sh "${env.WORKSPAC}/jenkins/scripts/test.sh"
                } finally {
                    sh "${env.WORKSPAC}/jenkins/scripts/kill.sh"
                }
            }
        }
    }
}